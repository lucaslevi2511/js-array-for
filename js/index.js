let alunos = ['Lucas', 'Camila', 'Joel'];

let mes = [8,4,10];
let ehEstudante= [true, false, false];
alunos.push('Nayara'); // forma correta de adicionar dados aos arrays

for(let i = 0; i < alunos.length; i++){
    mensagens.innerHTML += `<h1> Seja bem-vindo(a) ${alunos [i]} </h1>`;
}
